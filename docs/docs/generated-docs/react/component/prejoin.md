<!-- Do not edit this file. It is automatically generated by API Documenter. -->

# PreJoin

The PreJoin prefab component is normally presented to the user before he enters a room. This component allows the user to check and select the preferred media device (camera und microphone). On submit the user decisions are returned, which can then be passed on to the LiveKitRoom so that the user enters the room with the correct media devices.

## Import

```typescript
import { PreJoin } from '@livekit/components-react';
```

## Remarks

This component is independent from the LiveKitRoom component and don't has to be nested inside it. Because it only access the local media tracks this component is self contained and works without connection to the LiveKit server.

## Usage

```tsx
<PreJoin />
```

{% partial file="p_usage.md" /%}

## Properties

{% parameter name="camLabel" type="string" optional=true %}
_(Optional)_

{% /parameter %}

{% parameter name="debug" type="boolean" optional=true %}
_(Optional)_ Display a debug window for your convenience.

{% /parameter %}

{% parameter name="defaults" type="Partial<LocalUserChoices>" optional=true %}
_(Optional)_ Prefill the input form with initial values.

{% /parameter %}

{% parameter name="joinLabel" type="string" optional=true %}
_(Optional)_

{% /parameter %}

{% parameter name="micLabel" type="string" optional=true %}
_(Optional)_

{% /parameter %}

{% parameter name="onError" type="(error: Error) => void" optional=true %}
_(Optional)_

{% /parameter %}

{% parameter name="onSubmit" type="(values: LocalUserChoices) => void" optional=true %}
_(Optional)_ This function is called with the `LocalUserChoices` if validation is passed.

{% /parameter %}

{% parameter name="onValidate" type="(values: LocalUserChoices) => boolean" optional=true %}
_(Optional)_ Provide your custom validation function. Only if validation is successful the user choices are past to the onSubmit callback.

{% /parameter %}

{% parameter name="userLabel" type="string" optional=true %}
_(Optional)_

{% /parameter %}
