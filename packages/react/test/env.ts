/**
 * Environment variables related exclusively to tests.
 */

/** If set to true skip all performance tests that are not
 * "normal" test but rather performance investigations. */
export const SKIP_PERFORMANCE_TESTS = false;
